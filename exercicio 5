#include <stdio.h>

int main()
{
float preco, novo_preco;

printf("Digite o preco do produto: ");
scanf("%f", &preco);

novo_preco = preco - (preco * 0.10);

printf("o novo preco com desconto de 10 por cento eh: %.2f\n", novo_preco);

return 0;
}
